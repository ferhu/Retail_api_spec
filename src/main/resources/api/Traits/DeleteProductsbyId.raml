#%RAML 1.0 Trait
 responses:
   200:
     body:
       application/json:
        example: {"remove "}
   403:
     body:
       application/json:
         example:
          {"refuses to authorize it"}
   401:
     body:
       application/json:
        example: {"access is permanently forbidden"}